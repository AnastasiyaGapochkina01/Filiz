1) Написать плейбук, который:
- создает группу `developers`.
- создает пользователя `devuser` в группе `developers` с домашней директорией и shell `/bin/bash`
2) Написать плейбук, который:
- создает директорию `/opt/apps`.
- генерирует шаблонный файл `settings.j2` и размещает его как `/opt/apps/settings.conf`

содержимое файла `settings.j2`
```jinja2
server {{ app_server }}
port {{ app_port }}
```
3) Написать плейбук, который:
- создает директорию `/var/log/demo`.
- создает пустой файл `/var/log/demo/app.log` с правами `644` и владельцем `root`
4) Написать плейбук по установке chrony и запуску сервиса:
- устанавливает пакет `chrony`.
- запускает и включает сервис `chronyd` при загрузке
